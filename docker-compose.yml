version: '3'

volumes:
  data:

services:
  bot:
    image: jdan734-bot:dev
    build: 
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - data:/var/jdanbot
    environment:
      DP_PATH: /var/jdanbot/jdanbot.db
      TOKEN:
